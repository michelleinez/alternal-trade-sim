# alternal-trade-sim

brew install mongodb

In one terminal window do:
mongod

In another window do:
mongo

Then in a third do:
npm install mongoose
cd backbone_require
npm install
cd ..
node server.js
